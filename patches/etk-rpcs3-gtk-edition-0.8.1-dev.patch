diff --git a/.ci/deploy-linux.sh b/.ci/deploy-linux.sh
index 3309eb89e..272902316 100755
--- a/.ci/deploy-linux.sh
+++ b/.ci/deploy-linux.sh
@@ -20,7 +20,7 @@ if [ "$DEPLOY_APPIMAGE" = "true" ]; then
     APPIMAGE_EXTRACT_AND_RUN=1 linuxdeploy --appdir AppDir --plugin qt --plugin checkrt
 
     # Restore App Icon
-    ln -sr ./AppDir/rpcs3.svg ./AppDir/.DirIcon
+    ln -srf ./AppDir/rpcs3.svg ./AppDir/.DirIcon
     
     # Remove libwayland-client because it has platform-dependent exports and breaks other OSes
     rm -f ./AppDir/usr/lib/libwayland-client.so*
diff --git a/Utilities/Thread.cpp b/Utilities/Thread.cpp
index afeab41fe..132ffc630 100644
--- a/Utilities/Thread.cpp
+++ b/Utilities/Thread.cpp
@@ -1,5 +1,15 @@
 #include "stdafx.h"
 #include "Emu/System.h"
+
+#include <cstdlib>
+
+// Device-lost latch (Emu/RSX/VK/vkutils/shared.cpp). Forward-declared to keep
+// Vulkan headers out of core utilities; resolved at link in this fork's
+// always-Vulkan binary. Used by the stuck-join hard-exit below.
+namespace vk
+{
+	bool is_device_lost();
+}
 #include "Emu/Cell/SPUThread.h"
 #include "Emu/Cell/PPUThread.h"
 #include "Emu/Cell/lv2/sys_mmapper.h"
@@ -2860,7 +2870,25 @@ bool thread_base::join(bool dtor) const
 
 		if (i >= 16 && !(i & (i - 1)) && timeout != atomic_wait_timeout::inf)
 		{
-			sig_log.error("Thread [%s] is too sleepy. Waiting for it %.3fus already!", *m_tname.load(), (utils::get_tsc() - stamp0) / (utils::get_tsc_freq() / 1000000.));
+			const f64 waited_us = (utils::get_tsc() - stamp0) / (utils::get_tsc_freq() / 1000000.);
+			sig_log.error("Thread [%s] is too sleepy. Waiting for it %.3fus already!", *m_tname.load(), waited_us);
+
+			// Under a GPU device loss a stuck join is unwinnable in
+			// principle: a thread that emergency-exited on a DEVICE_LOST
+			// fatal dies WITHOUT unwinding, so any lock it held is orphaned
+			// (observed live 2026-07-04: PPU thread died holding the
+			// texture_cache shared_mutex; rsx::thread blocked on it forever;
+			// this join then waited on rsx doubling into the minutes). The
+			// process is stopping anyway — after 15 s, abandon the teardown
+			// outright. _Exit skips all remaining joins and destructors,
+			// which on a dead device is correct by construction (the kernel
+			// reclaims device objects and memory; persistent state was
+			// already flushed by the stop request).
+			if (waited_us > 15'000'000. && vk::is_device_lost())
+			{
+				sig_log.error("Device lost and join stuck for %.1fs: abandoning teardown, hard exit.", waited_us / 1000000.);
+				std::_Exit(0);
+			}
 		}
 	}
 
diff --git a/rpcs3/Emu/Cell/Modules/cellAudio.cpp b/rpcs3/Emu/Cell/Modules/cellAudio.cpp
index 557c7ef13..7a568d7b7 100644
--- a/rpcs3/Emu/Cell/Modules/cellAudio.cpp
+++ b/rpcs3/Emu/Cell/Modules/cellAudio.cpp
@@ -10,9 +10,71 @@
 #include "util/video_provider.h"
 
 #include <cmath>
+#include <cstdio>
+#include <ctime>
 
 LOG_CHANNEL(cellAudio);
 
+// =========================================================================
+// ETK GTK-Edition audio telemetry (aud1) — counters only, ZERO behavior
+// change. Backend underruns are otherwise silent zero/last-sample fill with
+// no log line at any level, so audible stutter leaves no forensic trace.
+// One raw text line is exported to /dev/shm/rpcs3_audio_stat every ~2 s for
+// the ETK ledger/HUD (atomic tmp+rename, ETK HUD law). Counters reset at
+// cellAudio thread start: a DRM-spawn title's second guest boot re-zeroes,
+// so end-of-session values describe the game proper, not the launcher.
+// =========================================================================
+struct etk_audio_stats_t
+{
+	atomic_t<u64> underrun_events{0}; // backend pulled more than the ring held (cubeb thread)
+	atomic_t<u64> underrun_bytes{0};  // total shortfall bytes (silence-filled downstream)
+	u64 skipped_periods = 0;          // cellAudio advanced past untouched buffers (game fell behind)
+	u64 silence_periods = 0;          // fully-untouched periods enqueued as silence
+	f32 ratio_min = 1.0f;             // lowest time-stretch frequency ratio since start
+	u64 start_us = 0;                 // emu clock at reset
+};
+static etk_audio_stats_t s_etk_aud{};
+
+static void etk_audio_stats_dump(u64 timestamp_us, f32 cur_ratio, u64 enqueued_playtime_us, u32 buf_ms)
+{
+#ifdef __linux__
+	char line[256];
+	const int n = std::snprintf(line, sizeof(line),
+		"up_s=%.1f ur=%llu urb=%llu skip=%llu sil=%llu ratio=%.3f rmin=%.3f enq_ms=%.1f buf_ms=%u\n",
+		(timestamp_us - s_etk_aud.start_us) / 1'000'000.0,
+		static_cast<unsigned long long>(s_etk_aud.underrun_events.load()),
+		static_cast<unsigned long long>(s_etk_aud.underrun_bytes.load()),
+		static_cast<unsigned long long>(s_etk_aud.skipped_periods),
+		static_cast<unsigned long long>(s_etk_aud.silence_periods),
+		cur_ratio, s_etk_aud.ratio_min,
+		enqueued_playtime_us / 1000.0, buf_ms);
+	if (n <= 0) return;
+
+	// Snapshot: latest values, atomically swapped (ETK ledger reads this at post-mortem)
+	if (std::FILE* f = std::fopen("/dev/shm/rpcs3_audio_stat.tmp", "w"))
+	{
+		std::fwrite(line, 1, static_cast<usz>(n), f);
+		std::fclose(f);
+		std::rename("/dev/shm/rpcs3_audio_stat.tmp", "/dev/shm/rpcs3_audio_stat");
+	}
+
+	// aud2: wall-clock-stamped timeline append — one line per dump, so a mixed
+	// session (TT -> ghost -> pack race) decomposes into phases post-hoc and
+	// correlates with the MangoHud fps autolog by epoch. Truncated at counter
+	// reset (fresh per guest boot); size-capped so an hours-long session can
+	// never balloon SHM (~150 KB/h at the 2 s cadence, 2 MB cap ≈ 13 h).
+	if (std::FILE* tl = std::fopen("/dev/shm/rpcs3_audio_log", "a"))
+	{
+		std::fseek(tl, 0, SEEK_END);
+		if (std::ftell(tl) < 2 * 1024 * 1024)
+		{
+			std::fprintf(tl, "ep=%llu %s", static_cast<unsigned long long>(std::time(nullptr)), line);
+		}
+		std::fclose(tl);
+	}
+#endif
+}
+
 extern atomic_t<recording_mode> g_recording_mode;
 
 extern void lv2_sleep(u64 timeout, ppu_thread* ppu = nullptr);
@@ -186,6 +248,12 @@ f32 audio_ringbuffer::set_frequency_ratio(f32 new_ratio)
 {
 	frequency_ratio = static_cast<f32>(resampler.set_tempo(new_ratio));
 
+	// ETK aud1: track deepest stretch engagement (cellAudio thread only)
+	if (frequency_ratio < s_etk_aud.ratio_min)
+	{
+		s_etk_aud.ratio_min = frequency_ratio;
+	}
+
 	return frequency_ratio;
 }
 
@@ -200,7 +268,17 @@ u32 audio_ringbuffer::backend_write_callback(u32 size, void *buf)
 {
 	if (!backend_active.observe()) backend_active = true;
 
-	return static_cast<u32>(cb_ringbuf.pop(buf, size, true));
+	const u32 popped = static_cast<u32>(cb_ringbuf.pop(buf, size, true));
+
+	// ETK aud1: the shortfall below is filled with silence/last-sample by the
+	// backend with no trace — this counter is the only witness. (cubeb thread)
+	if (popped < size)
+	{
+		s_etk_aud.underrun_events++;
+		s_etk_aud.underrun_bytes += size - popped;
+	}
+
+	return popped;
 }
 
 void audio_ringbuffer::backend_state_callback(AudioStateEvent event)
@@ -713,6 +791,18 @@ void cell_audio_thread::operator()()
 
 	u32 loop_count = 0;
 
+	// ETK aud1: fresh counters per guest boot (DRM-spawn second init re-zeroes)
+	s_etk_aud.underrun_events = 0;
+	s_etk_aud.underrun_bytes = 0;
+	s_etk_aud.skipped_periods = 0;
+	s_etk_aud.silence_periods = 0;
+	s_etk_aud.ratio_min = 1.0f;
+	s_etk_aud.start_us = get_system_time();
+	u64 etk_last_dump_us = 0;
+#ifdef __linux__
+	std::remove("/dev/shm/rpcs3_audio_log"); // aud2: fresh timeline per guest boot
+#endif
+
 	// Main cellAudio loop
 	while (thread_ctrl::state() != thread_state::aborting)
 	{
@@ -753,6 +843,14 @@ void cell_audio_thread::operator()()
 
 		const u64 time_since_last_period = timestamp - m_last_period_end;
 
+		// ETK aud1: telemetry export (~2 s cadence; /dev/shm write is memory-only)
+		if (timestamp - etk_last_dump_us > 2'000'000)
+		{
+			etk_audio_stats_dump(timestamp, ringbuffer->get_frequency_ratio(),
+				ringbuffer->get_enqueued_playtime(), static_cast<u32>(cfg.raw.desired_buffer_duration));
+			etk_last_dump_us = timestamp;
+		}
+
 		// Handle audio restart
 		if (m_audio_should_restart)
 		{
@@ -915,6 +1013,7 @@ void cell_audio_thread::operator()()
 				{
 					// There's no audio in the buffers, simply advance time and hope the game recovers
 					cellAudio.trace("advancing time: untouched=%u/%u (expected=%u), enqueued_buffers=%llu", untouched, active_ports, untouched_expected, enqueued_buffers);
+					s_etk_aud.skipped_periods++; // ETK aud1: game fell behind an audio period
 					untouched_expected = untouched;
 					advance(timestamp);
 					continue;
@@ -930,6 +1029,7 @@ void cell_audio_thread::operator()()
 			{
 				// There's no audio in the buffers, simply advance time
 				cellAudio.trace("enqueuing silence: untouched=%u/%u (expected=%u), enqueued_buffers=%llu", untouched, active_ports, untouched_expected, enqueued_buffers);
+				s_etk_aud.silence_periods++; // ETK aud1: a fully-silent period shipped to the backend
 				ringbuffer->enqueue_silence();
 				untouched_expected = untouched;
 				advance(timestamp);
@@ -1017,6 +1117,10 @@ void cell_audio_thread::operator()()
 		advance(timestamp);
 	}
 
+	// ETK aud1: final dump so post-mortem reads end-of-session values, not a ≤2s-stale line
+	etk_audio_stats_dump(get_system_time(), ringbuffer->get_frequency_ratio(),
+		ringbuffer->get_enqueued_playtime(), static_cast<u32>(cfg.raw.desired_buffer_duration));
+
 	// Destroy ringbuffer
 	ringbuffer.reset();
 
diff --git a/rpcs3/Emu/Cell/SPUCommonRecompiler.cpp b/rpcs3/Emu/Cell/SPUCommonRecompiler.cpp
index bc7190eab..8cb252b50 100644
--- a/rpcs3/Emu/Cell/SPUCommonRecompiler.cpp
+++ b/rpcs3/Emu/Cell/SPUCommonRecompiler.cpp
@@ -8827,11 +8827,6 @@ spu_program spu_recompiler_base::analyse(const be_t<u32>* ls, u32 entry_point, s
 		{
 			stats.single++;
 
-			if (true)
-			{
-				continue;
-			}
-
 			add_pattern(inst_attr::reduced_loop, loop_pc - result.entry_point, 0, std::make_shared<reduced_loop_t>(pattern));
 
 			std::string regs = "{";
diff --git a/rpcs3/Emu/Cell/SPULLVMRecompiler.cpp b/rpcs3/Emu/Cell/SPULLVMRecompiler.cpp
index ac22783ac..956ec0c34 100644
--- a/rpcs3/Emu/Cell/SPULLVMRecompiler.cpp
+++ b/rpcs3/Emu/Cell/SPULLVMRecompiler.cpp
@@ -9344,7 +9344,7 @@ public:
 
 		const auto a = get_vr(op.ra);
 
-		if (auto [ok, x, y] = match_expr(a, match<u32[4]>() + match<u32[4]>()); ok && false)
+		if (auto [ok, x, y] = match_expr(a, match<u32[4]>() + match<u32[4]>()); ok)
 		{
 			for (auto pair : std::initializer_list<std::pair<llvm_match_t<u32[4]>, llvm_match_t<u32[4]>>>{{x, y}, {y, x}})
 			{
@@ -9377,7 +9377,7 @@ public:
 		{
 			for (auto pair : std::initializer_list<std::pair<llvm_match_t<u32[4]>, llvm_match_t<u32[4]>>>{{x, y}, {y, x}})
 			{
-				if (auto [ok, data] = get_const_vector(pair.first.value, m_pos); ok && false)
+				if (auto [ok, data] = get_const_vector(pair.first.value, m_pos); ok)
 				{
 					// "sign extend" offset addend
 					const u64 addend = (data._u32[3] >= SPU_LS_SIZE) ? data._u32[3] | ~u64{SPU_LS_SIZE - 1} : data._u32[3];
diff --git a/rpcs3/Emu/Io/PadHandler.cpp b/rpcs3/Emu/Io/PadHandler.cpp
index 446358667..b52d0f739 100644
--- a/rpcs3/Emu/Io/PadHandler.cpp
+++ b/rpcs3/Emu/Io/PadHandler.cpp
@@ -444,11 +444,23 @@ void PadHandlerBase::TranslateButtonPress(const std::shared_ptr<PadDevice>& devi
 
 	if (get_is_left_trigger(device, keyCode))
 	{
+		// Top-end range calibration: a trigger that physically saturates below
+		// trigger_max can never report a full pull. When 'Left Trigger Max' is
+		// set (0 = uncalibrated), rescale so the calibrated ceiling maps to
+		// full deflection. Applied before the threshold compare.
+		if (const u32 cal_max = device->config->ltriggermax; cal_max > 0 && cal_max < trigger_max)
+		{
+			val = static_cast<u16>(std::min<u32>(trigger_max, static_cast<u32>(val) * trigger_max / cal_max));
+		}
 		pressed = val > (ignore_trigger_threshold ? 0 : device->config->ltriggerthreshold);
 		val = pressed ? NormalizeTriggerInput(val, device->config->ltriggerthreshold) : 0;
 	}
 	else if (get_is_right_trigger(device, keyCode))
 	{
+		if (const u32 cal_max = device->config->rtriggermax; cal_max > 0 && cal_max < trigger_max)
+		{
+			val = static_cast<u16>(std::min<u32>(trigger_max, static_cast<u32>(val) * trigger_max / cal_max));
+		}
 		pressed = val > (ignore_trigger_threshold ? 0 : device->config->rtriggerthreshold);
 		val = pressed ? NormalizeTriggerInput(val, device->config->rtriggerthreshold) : 0;
 	}
diff --git a/rpcs3/Emu/Io/pad_config.h b/rpcs3/Emu/Io/pad_config.h
index 86779c085..d7f7f5d6f 100644
--- a/rpcs3/Emu/Io/pad_config.h
+++ b/rpcs3/Emu/Io/pad_config.h
@@ -126,6 +126,11 @@ struct cfg_pad final : cfg::node
 	cfg::uint<0, 1000000> rstick_anti_deadzone{ this, "Right Stick Anti-Deadzone", 0 };
 	cfg::uint<0, 1000000> ltriggerthreshold{ this, "Left Trigger Threshold", 0 };
 	cfg::uint<0, 1000000> rtriggerthreshold{ this, "Right Trigger Threshold", 0 };
+	// Top-end range calibration: raw axis value (handler units) treated as a
+	// full pull. 0 = uncalibrated (stock behavior). For triggers that
+	// physically saturate below the handler's full range.
+	cfg::uint<0, 1000000> ltriggermax{ this, "Left Trigger Max", 0 };
+	cfg::uint<0, 1000000> rtriggermax{ this, "Right Trigger Max", 0 };
 	cfg::uint<0, 1000000> lpadsquircling{ this, "Left Pad Squircling Factor", 4000 };
 	cfg::uint<0, 1000000> rpadsquircling{ this, "Right Pad Squircling Factor", 4000 };
 
diff --git a/rpcs3/Emu/RSX/NV47/HW/nv406e.cpp b/rpcs3/Emu/RSX/NV47/HW/nv406e.cpp
index 3eaebb152..320ade4ed 100644
--- a/rpcs3/Emu/RSX/NV47/HW/nv406e.cpp
+++ b/rpcs3/Emu/RSX/NV47/HW/nv406e.cpp
@@ -3,13 +3,134 @@
 #include "nv47_sync.hpp"
 
 #include "Emu/RSX/RSXThread.h"
+#include "Utilities/Thread.h"
+#include "util/bless.hpp"
+
+#include <cstdlib>
 
 #include "context_accessors.define.h"
 
 namespace rsx
 {
+	namespace etk
+	{
+		// ETK KERS stage 1 — SEMA PARK counters (semapark-v1, 2026-07-12).
+		// Cumulative; the perfstat emitter (overlay_perf_metrics.cpp) extern-
+		// declares these and appends per-interval deltas to the SHM line —
+		// same cross-TU linkage idiom as vk::is_device_lost. Plain atomics:
+		// never a reason to fail, never on the R3 path.
+		atomic_t<u64> g_sema_acquires{0};   // acquire calls that had to wait
+		atomic_t<u64> g_sema_wakes{0};      // wait-loop iterations (one per WFE wake / stream tick)
+		atomic_t<u64> g_sema_parks{0};      // timed futex parks taken (non-flip)
+		atomic_t<u64> g_sema_parks_flip{0}; // timed futex parks taken on the FLIP semaphore (v2)
+	}
+
 	namespace nv406e
 	{
+		// ETK KERS stage 1 — SEMA PARK (semapark-v1, 2026-07-12; design:
+		// dossiers/KersConcept_20260712.md §3-K1).
+		//
+		// Evidence: on SM8250 pack racing this wait loop is the single
+		// hottest symbol in the process — 10.7-11.8% of ALL cycles across
+		// three 30 s perf samples (NPEA00502, 2026-07-12). The WFE inside
+		// spin_on_cacheline_once cannot honor its 100 µs bound on ARM (the
+		// comment in asm.hpp says so): wakes ride the kernel's ~107 µs
+		// arch-timer event stream AND cacheline invalidations — and GCM
+		// labels are 16 bytes apart, so FOUR guest labels share each 64 B
+		// monitored line. Six SPU threads hammering reservations turn the
+		// "wait" into a hot spin of spurious wakes.
+		//
+		// Mitigation: once the spin has burned GTK_SEMA_SPIN_US (default 150)
+		// without the semaphore changing, escalate each iteration to a timed
+		// futex park of GTK_SEMA_PARK_US (default 200) — a real kernel sleep.
+		// Releases are plain vm::write with no futex notify, so the park is
+		// timeout-bounded by construction; worst case it adds one park period
+		// to an acquire that was already >150 µs old (≤0.6% of a 33 ms frame).
+		// The FLIP semaphore (label_addr+0x10) is deliberately excluded: it is
+		// the wedge-prone path the anti-lock nets watch, and it must keep the
+		// tightest possible reaction time (KersConcept falsifier (b)).
+		// The full loop preamble (stop/pause/timeout checks and
+		// on_semaphore_acquire_wait — queued flips, flush requests) re-runs
+		// after every park, so pending local work is serviced at least once
+		// per park period.
+		//
+		// semapark-v2 (2026-07-13, GTK Edition v0.7.3): v1's flip-semaphore
+		// exclusion exempted exactly the wait that burns the machine. The v1
+		// counters proved it on-rig: during racing sq~0 everywhere EXCEPT the
+		// flip label — one NPEA00502 interval logged sw=5,554,102 with pk=0
+		// (a ~13-16M laps/s monitor-clear hot spin, hours of the false-sharing
+		// storm the doc block above predicted), while the one eligible
+		// non-flip wait parked at 89% efficiency (sw=4983 -> pk=4451). v2
+		// therefore parks the flip semaphore too, under its own tighter bound
+		// GTK_SEMA_PARK_FLIP_US (default 100 — <=0.6% of a 16.7 ms frame in
+		// added flip-detection latency) and its own kill-switch
+		// GTK_SEMA_PARK_FLIP=0 (restores the v1 exclusion). During a real
+		// 00E5 wedge the flip label never signals at all — the anti-lock nets
+		// own that; parking merely stops the doomed wait from cooking a core.
+		//
+		// Default ON (house law: new-build features ship on, kill-switch for
+		// A/B): GTK_SEMA_PARK=0 disables, restoring stock WFE-spin behavior.
+		static bool etk_sema_park_enabled()
+		{
+			static const bool enabled = []()
+			{
+				const char* v = std::getenv("GTK_SEMA_PARK");
+				return !(v && *v == '0');
+			}();
+			return enabled;
+		}
+
+		static u64 etk_sema_spin_us()
+		{
+			static const u64 us = []() -> u64
+			{
+				if (const char* v = std::getenv("GTK_SEMA_SPIN_US"))
+				{
+					const u64 parsed = std::strtoull(v, nullptr, 10);
+					if (parsed >= 1 && parsed <= 1'000'000) return parsed;
+				}
+				return 150;
+			}();
+			return us;
+		}
+
+		static u64 etk_sema_park_us()
+		{
+			static const u64 us = []() -> u64
+			{
+				if (const char* v = std::getenv("GTK_SEMA_PARK_US"))
+				{
+					const u64 parsed = std::strtoull(v, nullptr, 10);
+					if (parsed >= 50 && parsed <= 10'000) return parsed;
+				}
+				return 200;
+			}();
+			return us;
+		}
+
+		static bool etk_sema_park_flip_enabled()
+		{
+			static const bool enabled = []()
+			{
+				const char* v = std::getenv("GTK_SEMA_PARK_FLIP");
+				return !(v && *v == '0');
+			}();
+			return enabled;
+		}
+
+		static u64 etk_sema_park_flip_us()
+		{
+			static const u64 us = []() -> u64
+			{
+				if (const char* v = std::getenv("GTK_SEMA_PARK_FLIP_US"))
+				{
+					const u64 parsed = std::strtoull(v, nullptr, 10);
+					if (parsed >= 50 && parsed <= 1'000) return parsed;
+				}
+				return 100;
+			}();
+			return us;
+		}
 		void set_reference(context* ctx, u32 /*reg*/, u32 arg)
 		{
 			RSX(ctx)->sync();
@@ -29,7 +150,7 @@ namespace rsx
 			RSX(ctx)->m_graphics_state |= rsx::pipeline_state::fragment_program_needs_rehash;
 
 			const auto& sema = vm::_ref<RsxSemaphore>(addr);
-			const auto& atomic_sema = vm::_ref<atomic_t<RsxSemaphore>>(addr);
+			const auto& atomic_sema = vm::_ref<atomic_t<RsxSemaphore>>(addr);
 
 			if (sema == arg)
 			{
@@ -50,8 +171,17 @@ namespace rsx
 			u64 start = get_system_time();
 			u64 last_check_val = start;
 
+			// ETK semapark-v2 (doc block above): the flip semaphore parks under
+			// its own tighter bound; everything else under the standard one.
+			const bool is_flip_sema = (addr == RSX(ctx)->label_addr + 0x10);
+			const bool sema_park_eligible = etk_sema_park_enabled() && (!is_flip_sema || etk_sema_park_flip_enabled());
+			const u64 sema_park_period_us = is_flip_sema ? etk_sema_park_flip_us() : etk_sema_park_us();
+			etk::g_sema_acquires++;
+
 			while (sema != arg)
 			{
+				etk::g_sema_wakes++;
+
 				if (RSX(ctx)->test_stopped())
 				{
 					RSX(ctx)->state += cpu_flag::again;
@@ -80,17 +210,31 @@ namespace rsx
 					}
 				}
 
-				if (RSX(ctx)->external_interrupt_lock ||
-					(RSX(ctx)->state & (cpu_flag::dbg_global_pause + cpu_flag::exit)) == cpu_flag::dbg_global_pause)
-				{
-					RSX(ctx)->cpu_wait({});
-					continue;
-				}
-
-				RSX(ctx)->on_semaphore_acquire_wait();
-
-				// Wait until the value changes or until 100us pass.
-				utils::spin_on_cacheline_once(atomic_sema, sema, 100);
+				if (RSX(ctx)->external_interrupt_lock ||
+					(RSX(ctx)->state & (cpu_flag::dbg_global_pause + cpu_flag::exit)) == cpu_flag::dbg_global_pause)
+				{
+					RSX(ctx)->cpu_wait({});
+					continue;
+				}
+
+				RSX(ctx)->on_semaphore_acquire_wait();
+
+				// ETK semapark-v1: past the spin budget, take a real kernel
+				// sleep instead of another lap of spurious WFE wakes. The
+				// futex old-value is the word's live bits (any change is a
+				// reason to re-check); nobody notifies this address, so the
+				// park returns at timeout (or on thread sync/task signals,
+				// which wait_on also arms) and the loop re-runs the preamble.
+				if (sema_park_eligible && (get_system_time() - start) >= etk_sema_spin_us())
+				{
+					(is_flip_sema ? etk::g_sema_parks_flip : etk::g_sema_parks)++;
+					auto* wait_word = utils::bless<atomic_t<u32>>(&const_cast<atomic_t<RsxSemaphore>&>(atomic_sema));
+					thread_ctrl::wait_on(*wait_word, wait_word->load(), sema_park_period_us);
+					continue;
+				}
+
+				// Wait until the value changes or until 100us pass.
+				utils::spin_on_cacheline_once(atomic_sema, sema, 100);
 			}
 
 			RSX(ctx)->fifo_wake_delay();
diff --git a/rpcs3/Emu/RSX/Overlays/overlay_perf_metrics.cpp b/rpcs3/Emu/RSX/Overlays/overlay_perf_metrics.cpp
index a3a53dc86..e8240dcb7 100644
--- a/rpcs3/Emu/RSX/Overlays/overlay_perf_metrics.cpp
+++ b/rpcs3/Emu/RSX/Overlays/overlay_perf_metrics.cpp
@@ -11,6 +11,83 @@
 
 #include "util/cpu_stats.hpp"
 
+#include <cstdio>
+#include <ctime>
+
+// ETK semapark-v1 counters (Emu/RSX/NV47/HW/nv406e.cpp). Forward-declared to
+// keep RSX HW headers out of the overlay TU; linked from the same binary —
+// the vk::is_device_lost idiom.
+namespace rsx
+{
+	namespace etk
+	{
+		extern atomic_t<u64> g_sema_acquires;
+		extern atomic_t<u64> g_sema_wakes;
+		extern atomic_t<u64> g_sema_parks;
+		extern atomic_t<u64> g_sema_parks_flip;
+		// v0.7.4 (RSXThread.cpp): access-violation fault counters + ffs-v5
+		// flip retires.
+		extern atomic_t<u64> g_av_write_faults;
+		extern atomic_t<u64> g_av_read_faults;
+		extern atomic_t<u64> g_flip_retires;
+	}
+}
+
+namespace rsx
+{
+	namespace overlays
+	{
+		// ETK perf-stat SHM emit (v0.7.1, perfstat-v1). Mirror the overlay's OWN
+		// computed per-subsystem usage to /dev/shm so the ledger can attribute a
+		// slow frame (pack-bog: PPU/SPU/RSX split) with no GUI and no MangoHud.
+		// Same tmp+rename + bounded append-log pattern as the aud= emitter.
+		// fail-silent; Linux only (the rig). Values are exactly what the overlay
+		// draws, so ledger and screen can never disagree.
+		// semapark-v1 addendum: sq/sw/pk = SEMA PARK deltas since the previous
+		// emit (acquire-waits entered / wake iterations / futex parks) — the
+		// K1 instrument. sw collapsing toward pk = the spin is parking; a huge
+		// sw with pk=0 = kill-switch active or spin budget never reached.
+		static void etk_perf_stats_dump(f32 fps, f32 ft_ms, f32 cpu,
+			f32 ppu, f32 spu, f32 rsx, u32 rsx_load, u32 threads)
+		{
+#ifdef __linux__
+			static u64 prev_sq = 0, prev_sw = 0, prev_pk = 0, prev_pf = 0, prev_av = 0;
+			const u64 cur_sq = etk::g_sema_acquires.load();
+			const u64 cur_sw = etk::g_sema_wakes.load();
+			const u64 cur_pk = etk::g_sema_parks.load();
+			const u64 cur_pf = etk::g_sema_parks_flip.load();
+			const u64 cur_av = etk::g_av_write_faults.load() + etk::g_av_read_faults.load();
+			const u64 cur_fr = etk::g_flip_retires.load();
+			const u64 d_sq = cur_sq - prev_sq, d_sw = cur_sw - prev_sw, d_pk = cur_pk - prev_pk, d_pf = cur_pf - prev_pf, d_av = cur_av - prev_av;
+			prev_sq = cur_sq; prev_sw = cur_sw; prev_pk = cur_pk; prev_pf = cur_pf; prev_av = cur_av;
+
+			char line[352];
+			const int n = std::snprintf(line, sizeof(line),
+				"fps=%.1f ft_ms=%.2f cpu=%.1f ppu=%.1f spu=%.1f rsx=%.1f rsxload=%u thr=%u sq=%llu sw=%llu pk=%llu pf=%llu av=%llu fr=%llu\n",
+				fps, ft_ms, cpu, ppu, spu, rsx, rsx_load, threads,
+				static_cast<unsigned long long>(d_sq), static_cast<unsigned long long>(d_sw), static_cast<unsigned long long>(d_pk),
+				static_cast<unsigned long long>(d_pf), static_cast<unsigned long long>(d_av), static_cast<unsigned long long>(cur_fr));
+			if (n <= 0) return;
+
+			if (std::FILE* f = std::fopen("/dev/shm/rpcs3_perf_stat.tmp", "w"))
+			{
+				std::fwrite(line, 1, static_cast<usz>(n), f);
+				std::fclose(f);
+				std::rename("/dev/shm/rpcs3_perf_stat.tmp", "/dev/shm/rpcs3_perf_stat");
+			}
+
+			if (std::FILE* tl = std::fopen("/dev/shm/rpcs3_perf_log", "a"))
+			{
+				std::fseek(tl, 0, SEEK_END);
+				if (std::ftell(tl) < 2 * 1024 * 1024)
+					std::fprintf(tl, "ep=%llu %s", static_cast<unsigned long long>(std::time(nullptr)), line);
+				std::fclose(tl);
+			}
+#endif
+		}
+	}
+}
+
 namespace rsx
 {
 	namespace overlays
@@ -577,6 +654,17 @@ namespace rsx
 					}
 				}
 
+				// ETK: mirror the just-computed frame split to SHM for the ledger.
+				// Detail=High is the only level where the full record is fresh
+				// (high -> medium fallthrough sets ppu/spu/rsx; high itself sets
+				// frametime/rsx_load/threads). Operator arms High via the Pitstop
+				// Perf Overlay dials for a pack/audio attribution stint.
+				if (m_detail == detail_level::high)
+				{
+					etk_perf_stats_dump(m_fps, m_frametime, m_cpu_usage,
+						m_ppu_usage, m_spu_usage, m_rsx_usage, m_rsx_load, m_total_threads);
+				}
+
 				// 2. Format output string
 				std::string perf_text;
 
diff --git a/rpcs3/Emu/RSX/RSXTexture.cpp b/rpcs3/Emu/RSX/RSXTexture.cpp
index e0c061724..c4748f1c4 100644
--- a/rpcs3/Emu/RSX/RSXTexture.cpp
+++ b/rpcs3/Emu/RSX/RSXTexture.cpp
@@ -243,6 +243,42 @@ namespace rsx
 	rsx::texture_channel_remap_t fragment_texture::decoded_remap() const
 	{
 		u32 remap_ctl = registers[NV4097_SET_TEXTURE_CONTROL1 + (m_index * 8)];
+
+		// ETK GTK experiment (11912 road-flicker, dossier Tier0_11912_ShadowRemapLocalization_20260702):
+		// GT5P parks its shadow-map TIU on a 1x1 fallback with a fully zeroed CONTROL1 (= FORCE-ZERO x4
+		// per the GCM enum) -> every track draw samples a constant-0 shadow-visibility term and the
+		// whole track renders shadowed; real hardware evidently treats the parked state as neutral.
+		// v2 (2026-07-02, after on-rig A/B): scoped to the exact confirmed signature — fully zeroed
+		// low word AND a 1x1 texture (on-rig probe showed the 1x1 is the ONLY zero-remap texture in
+		// live GT5P). Two decodes discriminate what console "neutral" means:
+		//   ONE x4            -> content-independent "no shadow" (kills content-staleness blips
+		//                        if the 1x1's texel is meaningless on console)
+		//   identity crossbar -> samples the 1x1's live content (correct if the game legitimately
+		//                        animates it as a global dimmer, e.g. tunnels)
+		// v1's blanket zero-control-field override caused short new flicker blips and is retired.
+		// v3 (0.6.0 GA): the ONE decode is ON BY DEFAULT — it is the hardware-matching
+		// interpretation (field-validated flicker-free, Eiger/Daytona) and the old opt-in gate
+		// was a leftover of the A/B bench, not a decision. Runtime controls:
+		//   GTK_REMAP0_ONE=0      -> kill-switch: stock zero-decode (diagnosis/regression A/B)
+		//   GTK_REMAP0_IDENTITY=1 -> identity-crossbar A/B variant
+		//   GTK_REMAP0_ONE=1      -> explicit ONE; outranks IDENTITY (preserves the v2 contract)
+		static const char* const s_gtk_remap0_env = std::getenv("GTK_REMAP0_ONE");
+		static const bool g_gtk_remap0_one_explicit = s_gtk_remap0_env && *s_gtk_remap0_env == '1';
+		static const bool g_gtk_remap0_off = s_gtk_remap0_env && *s_gtk_remap0_env == '0';
+		static const bool g_gtk_remap0_identity = []() { const char* e = std::getenv("GTK_REMAP0_IDENTITY"); return e && *e == '1'; }();
+		const bool gtk_use_identity = g_gtk_remap0_identity && !g_gtk_remap0_one_explicit;
+		if ((!g_gtk_remap0_off || g_gtk_remap0_identity) && (remap_ctl & 0xFFFF) == 0
+			&& width() <= 1 && height() <= 1) [[unlikely]]
+		{
+			static atomic_t<bool> s_logged = false;
+			if (!s_logged.exchange(true))
+			{
+				rsx_log.warning("GTK-REMAP0: %s override engaged (TIU%u raw=0x%08x)",
+					gtk_use_identity ? "identity" : "force-ONE", m_index, remap_ctl);
+			}
+			remap_ctl = (remap_ctl & 0xFFFF0000u) | (gtk_use_identity ? 0xAAE4u : 0x55E4u);
+		}
+
 		u32 remap_override = (remap_ctl >> 16) & 0xFFFF;
 
 		switch (format() & ~(CELL_GCM_TEXTURE_LN | CELL_GCM_TEXTURE_UN))
diff --git a/rpcs3/Emu/RSX/RSXThread.cpp b/rpcs3/Emu/RSX/RSXThread.cpp
index 7f42689cb..b1048d7a1 100644
--- a/rpcs3/Emu/RSX/RSXThread.cpp
+++ b/rpcs3/Emu/RSX/RSXThread.cpp
@@ -21,6 +21,8 @@
 #include "Emu/Cell/lv2/sys_time.h"
 #include "Emu/Cell/Modules/cellGcmSys.h"
 #include "util/serialization_ext.hpp"
+
+#include <cstdlib>
 #include "Overlays/overlay_perf_metrics.h"
 #include "Overlays/overlay_debug_overlay.h"
 #include "Overlays/overlay_manager.h"
@@ -115,6 +117,13 @@ bool serialize<rsx::rsx_iomap_table>(utils::serial& ar, rsx::rsx_iomap_table& o)
 	return true;
 }
 
+// ffs-v5: forced-fence counter accessor (vkutils/sync.cpp). Forward-declared
+// to keep Vulkan headers out — the vk::is_device_lost linkage idiom.
+namespace vk
+{
+	u64 etk_fence_forced_count();
+}
+
 namespace rsx
 {
 	std::function<bool(u32 addr, bool is_writing)> g_access_violation_handler;
@@ -926,6 +935,157 @@ namespace rsx
 		void set_render_mode(context* rsx, u32, u32 arg);
 	}
 
+	// ETK anti-lock stage 2 — the RSX progress watchdog (ffs-v3, 2026-07-06).
+	// Defeat Class #4, proven live twice in one day (GT HD Eiger + GT5P
+	// HSL-reverse, identical gdb signatures): a kernel keepalive rescue drops
+	// the faulting submit's guest-visible completion (ZCULL report / label),
+	// the game's render loop parks polling for it, the FIFO goes permanently
+	// EMPTY, and rsx idle-yields forever while the rest of the guest keeps
+	// simulating headless. No VkFence is stale, so the fence force-signal net
+	// is structurally blind here. THIS net watches FIFO progress instead:
+	//   stage A (idle > threshold, default 10 s, in-game, not paused):
+	//     log + zcull_ctrl->sync() — force-writes every claimed pending
+	//     report; a query killed by the rescue is forged to zero by the
+	//     GTK driver's TU_ETK_QUERY_SURVIVE instead of blocking, so the
+	//     sink write lands and the polling guest thread unparks.
+	//   stage B (still idle > 2x threshold): give up cleanly — Emu.Kill,
+	//     so the session self-recovers to a ledger row without an R3.
+	// Gated on GTK_RSX_WATCHDOG=1 (threshold override GTK_RSX_WATCHDOG_S);
+	// flag off = zero behavior change.
+	static bool etk_rsx_watchdog_enabled()
+	{
+		static const bool enabled = []()
+		{
+			const char* v = std::getenv("GTK_RSX_WATCHDOG");
+			return !(v && *v == '0'); // ETK v0.7.0: on by default; "0" = kill-switch
+		}();
+		return enabled;
+	}
+
+	static u64 etk_rsx_watchdog_threshold_us()
+	{
+		static const u64 threshold = []() -> u64
+		{
+			if (const char* v = std::getenv("GTK_RSX_WATCHDOG_S"))
+			{
+				if (const u64 s = std::strtoull(v, nullptr, 10))
+				{
+					return s * 1'000'000ull;
+				}
+			}
+			// 10 s: a healthy game never leaves the FIFO empty this long
+			// (menus, loading bars and replays all keep pushing); a class-4
+			// park is permanent, so extra latency only delays the rescue.
+			return 10'000'000ull;
+		}();
+		return threshold;
+	}
+
+	// ETK anti-lock stage 3b — FIFO-desync resync net (ffs-v4, 2026-07-06).
+	// Defeat Class #3 (FIFO-desync rsx-death), captured live with a full gdb
+	// bundle (manual_forensics/class3_fifodeath_*): a burst of kernel keepalive
+	// rescues drops many submits' guest-visible effects; the resulting FIFO
+	// desync STORM trips RPCS3's own "hopeless" give-up in recover_fifo() (20
+	// recoveries in < 2 s), which THROWS and kills rsx::thread. With rsx dead the
+	// guest deadlocks alive (SPUs parked in spu_thread::get_ch_value awaiting
+	// reports that never land). No VkFence is stale (ffs-v2 blind) and the FIFO
+	// never goes idle-EMPTY — it goes desynced-DEAD — so the ffs-v3 idle watchdog
+	// is blind too AND dies with the thread it rides. This net lives INSIDE
+	// recover_fifo: when the give-up would fire, force a ZCULL report sync (unpark
+	// the polling SPUs; a rescue-killed query is forged to zero by the GTK driver's
+	// TU_ETK_QUERY_SURVIVE instead of blocking) and run the existing queue-reset
+	// recovery instead of throwing, bounded to N consecutive give-ups; only if we
+	// still cannot make progress do we stop CLEANLY (ledger row, no R3) rather than
+	// freeze. Gated on GTK_FIFO_RESYNC=1 (budget override GTK_FIFO_RESYNC_TRIES);
+	// flag off = bit-for-bit stock throw. Composes with the keepalive kernel + the
+	// fence force-signal; it is the net for drops that CORRUPT the FIFO stream
+	// rather than merely STARVE it.
+	static bool etk_fifo_resync_enabled()
+	{
+		static const bool enabled = []()
+		{
+			const char* v = std::getenv("GTK_FIFO_RESYNC");
+			return !(v && *v == '0'); // ETK v0.7.0: on by default; "0" = kill-switch
+		}();
+		return enabled;
+	}
+
+	static u32 etk_fifo_resync_budget()
+	{
+		static const u32 budget = []() -> u32
+		{
+			if (const char* v = std::getenv("GTK_FIFO_RESYNC_TRIES"))
+			{
+				if (const u32 n = static_cast<u32>(std::strtoul(v, nullptr, 10)))
+				{
+					return n;
+				}
+			}
+			// 10 consecutive give-up hits (each = 20 rapid recoveries) rides out
+			// a keepalive submit-drop storm without an unbounded frozen spin.
+			return 10u;
+		}();
+		return budget;
+	}
+
+	namespace etk
+	{
+		// ETK v0.7.4 counters (perfstat-surfaced; overlay_perf_metrics.cpp
+		// extern-declares them — the vk::is_device_lost linkage idiom).
+		atomic_t<u64> g_flip_retires{0};    // ffs-v5 forced flip retires
+		atomic_t<u64> g_av_write_faults{0}; // access-violation write faults (VKGSRender)
+		atomic_t<u64> g_av_read_faults{0};  // access-violation read faults
+	}
+
+	// ETK anti-lock stage 4 — flip-status force-retire (ffs-v5, 2026-07-12,
+	// GTK Edition v0.7.4). The remaining ~50% RECOVERY share on 00E59005
+	// (N>=6 in one weekend): a keepalive rescue drops the submit whose
+	// completion would have flipped; flip_status stays WAITING forever and
+	// the guest parks in its cellGcmGetFlipStatus poll (GT5P EMAIN 0xbc92d0,
+	// lifted by ps3probe's flip lens). Nothing below can help — the fence
+	// force-signal nets operate at the Vulkan layer, but the guest-visible
+	// LOSS already happened. The retire runs on the rsx thread (the same
+	// thread handle_emu_flip runs on) and replays exactly its completion
+	// tail: flip_status -> DONE + fire the registered flip handler. Gated on
+	// a rescue having occurred inside the WAITING window (forced-fence count
+	// delta, stage-2 arming, or a FIFO-desync streak) so an organic long
+	// wait (loading screen) is never touched; GTK_FLIP_RETIRE_ANY=1 drops
+	// the gate for diagnosis. Default ON (house law), GTK_FLIP_RETIRE=0
+	// kill-switch, threshold GTK_FLIP_RETIRE_S (default 3 s).
+	static bool etk_flip_retire_enabled()
+	{
+		static const bool enabled = []()
+		{
+			const char* v = std::getenv("GTK_FLIP_RETIRE");
+			return !(v && *v == '0');
+		}();
+		return enabled;
+	}
+
+	static u64 etk_flip_retire_threshold_us()
+	{
+		static const u64 us = []() -> u64
+		{
+			if (const char* v = std::getenv("GTK_FLIP_RETIRE_S"))
+			{
+				const u64 parsed = std::strtoull(v, nullptr, 10);
+				if (parsed >= 1 && parsed <= 120) return parsed * 1'000'000;
+			}
+			return 3'000'000;
+		}();
+		return us;
+	}
+
+	static bool etk_flip_retire_any()
+	{
+		static const bool any = []()
+		{
+			const char* v = std::getenv("GTK_FLIP_RETIRE_ANY");
+			return v && *v == '1';
+		}();
+		return any;
+	}
+
 	void thread::on_task()
 	{
 		g_tls_log_prefix = []
@@ -1120,6 +1280,15 @@ namespace rsx
 			manager->stop_audio();
 		}
 
+		// ETK anti-lock stage 2 state (see the doc block above on_task).
+		// 0 = armed, 1 = stage A fired (zcull force-sync done), 2 = gave up.
+		u32 etk_wd_stage = 0;
+
+		// ETK anti-lock stage 4 state (ffs-v5): the continuous flip-WAITING
+		// window and the rescue-counter snapshot taken when it opened.
+		u64 etk_flip_wait_mark = 0;
+		u64 etk_fence_forced_at_mark = 0;
+
 		while (!test_stopped())
 		{
 			// Wait for external pause events
@@ -1156,6 +1325,103 @@ namespace rsx
 				{
 					m_host_dma_ctrl->update();
 				}
+
+				// ETK anti-lock stage 2 — RSX progress watchdog (doc block
+				// above on_task). Two u64 compares per 64 cycles when armed.
+				if (etk_rsx_watchdog_enabled() && int_flip_index > 0 && !Emu.IsPaused())
+				{
+					if (performance_counters.state == FIFO::state::running)
+					{
+						// FIFO progressed — re-arm.
+						etk_wd_stage = 0;
+					}
+					else if (etk_wd_stage < 2)
+					{
+						const u64 idle_us = get_system_time() - performance_counters.FIFO_idle_timestamp;
+						const u64 threshold = etk_rsx_watchdog_threshold_us();
+
+						if (etk_wd_stage == 0 && idle_us > threshold)
+						{
+							etk_wd_stage = 1;
+							rsx_log.error("ETK anti-lock stage2: FIFO starved for %llums in-game (state=%d get=0x%x put=0x%x flip#%llu) - forcing ZCULL report sync (post-rescue guest park suspected).",
+								idle_us / 1000, static_cast<u32>(performance_counters.state), +ctrl->get, +ctrl->put, int_flip_index);
+							// Force-write every claimed pending report. A query
+							// whose GPU work died in a keepalive rescue is forged
+							// to zero by the GTK driver (TU_ETK_QUERY_SURVIVE)
+							// instead of blocking this thread, so the guest's
+							// polled sink gets its write and can unpark.
+							zcull_ctrl->sync(this);
+							rsx_log.error("ETK anti-lock stage2: ZCULL sync returned - waiting for FIFO progress.");
+						}
+						else if (etk_wd_stage == 1 && idle_us > threshold * 2)
+						{
+							etk_wd_stage = 2;
+							rsx_log.fatal("ETK anti-lock stage2: FIFO still starved %llums after forced ZCULL sync - guest state is unrecoverable, stopping emulation cleanly (no R3 needed).",
+								idle_us / 1000);
+							Emu.Kill(false, false);
+						}
+					}
+				}
+
+				// ETK anti-lock stage 4 — flip-status force-retire (ffs-v5;
+				// doc block above the flag getters). Two compares per 64
+				// cycles when healthy.
+				if (etk_flip_retire_enabled() && int_flip_index > 0 && !Emu.IsPaused())
+				{
+					if (flip_status != CELL_GCM_DISPLAY_FLIP_STATUS_WAITING)
+					{
+						etk_flip_wait_mark = 0;
+					}
+					else if (!etk_flip_wait_mark)
+					{
+						etk_flip_wait_mark = get_system_time();
+						etk_fence_forced_at_mark = vk::etk_fence_forced_count();
+					}
+					else if (get_system_time() - etk_flip_wait_mark > etk_flip_retire_threshold_us())
+					{
+						const bool rescued = vk::etk_fence_forced_count() != etk_fence_forced_at_mark
+							|| etk_wd_stage != 0 || etk_fifo_dead_streak != 0;
+
+						if (rescued || etk_flip_retire_any())
+						{
+							const u64 n = ++etk::g_flip_retires;
+							rsx_log.error("ETK anti-lock stage4 (ffs-v5): guest flip WAITING %llums across a rescue - force-retiring flip status (retire #%llu, flip#%llu, forced_fences=%llu).",
+								(get_system_time() - etk_flip_wait_mark) / 1000, n, int_flip_index, vk::etk_fence_forced_count());
+
+							// Replay handle_emu_flip's completion tail: the
+							// status write the guest polls, then its flip
+							// handler interrupt. Same thread, same order.
+							flip_status = CELL_GCM_DISPLAY_FLIP_STATUS_DONE;
+
+							if (!isHLE)
+							{
+								sys_rsx_context_attribute(0x55555555, 0xFEC, current_display_buffer, 0, 0, 0);
+							}
+							else if (auto ptr = flip_handler)
+							{
+								intr_thread->cmd_list
+								({
+									{ ppu_cmd::set_args, 1 }, u64{ 1 },
+									{ ppu_cmd::lle_call, ptr },
+									{ ppu_cmd::sleep, 0 }
+								});
+
+								intr_thread->cmd_notify.store(1);
+								intr_thread->cmd_notify.notify_one();
+							}
+
+							etk_flip_wait_mark = 0;
+						}
+						else
+						{
+							// No rescue inside the window — an organic long
+							// wait (loading screen). Re-anchor so a LATER
+							// rescue is measured against a fresh window.
+							etk_flip_wait_mark = get_system_time();
+							etk_fence_forced_at_mark = vk::etk_fence_forced_count();
+						}
+					}
+				}
 			}
 
 			// Execute FIFO queue
@@ -2791,7 +3057,16 @@ namespace rsx
 
 	void thread::recover_fifo(std::source_location src_loc)
 	{
-		bool kill_itself = g_cfg.core.rsx_fifo_accuracy == rsx_fifo_mode::as_ps3;
+		// Config-driven strict death (RSX FIFO Accuracy = "PS3"): the user asked
+		// for accurate die-on-desync — ETK never overrides this.
+		const bool cfg_kill = g_cfg.core.rsx_fifo_accuracy == rsx_fifo_mode::as_ps3;
+		bool kill_itself = cfg_kill;
+
+		// Storm-driven give-up: 20 recoveries inside ~2 s = "probably hopeless".
+		// This is the branch a keepalive submit-drop burst trips (see the
+		// ffs-v4 doc block above). Tracked separately so the ETK net only ever
+		// touches THIS give-up, never the config-accurate one.
+		bool storm_kill = false;
 
 		const u64 current_time = get_system_time();
 
@@ -2804,13 +3079,52 @@ namespace rsx
 			if (current_time - cmd_info.timestamp < 2'000'000u - std::min<u32>(g_cfg.video.driver_wakeup_delay * 700, 1'400'000))
 			{
 				// Probably hopeless
-				kill_itself = true;
+				storm_kill = true;
 			}
 
 			// Erase the last command from history, keep the size of the queue the same
 			recovered_fifo_cmds_history.pop();
 		}
 
+		// ETK anti-lock stage 3b (ffs-v4): intercept the storm give-up before it
+		// throws and kills rsx::thread. Only the storm branch, and only when the
+		// config did not itself ask to die.
+		if (etk_fifo_resync_enabled() && storm_kill && !cfg_kill)
+		{
+			if (++etk_fifo_dead_streak > etk_fifo_resync_budget())
+			{
+				rsx_log.fatal("ETK anti-lock stage3b: FIFO desync unrecoverable after %u forced resyncs (keepalive submit-drop storm) - stopping emulation cleanly (no R3 needed).",
+					etk_fifo_dead_streak);
+				Emu.Kill(false, false);
+				// Fall through to the queue reset so this thread unwinds tidily;
+				// the run_FIFO loop's test_stopped() then exits it. Do NOT throw.
+				storm_kill = false;
+			}
+			else
+			{
+				rsx_log.error("ETK anti-lock stage3b: dead-FIFO give-up suppressed (%u/%u) - forcing ZCULL report sync + queue resync to get=0x%x, continuing.",
+					etk_fifo_dead_streak, etk_fifo_resync_budget(), restore_point);
+				// Force-write every claimed pending report so SPUs parked in
+				// get_ch_value (awaiting a report the dropped submit never wrote)
+				// unpark. A query whose GPU work died in a keepalive rescue is
+				// forged to zero by the GTK driver (TU_ETK_QUERY_SURVIVE) instead
+				// of blocking this thread.
+				if (zcull_ctrl)
+				{
+					zcull_ctrl->sync(this);
+				}
+				// Suppress the throw; fall through to the queue reset below.
+				storm_kill = false;
+			}
+		}
+		else if (!storm_kill)
+		{
+			// Recoveries have spread out (real progress between them) — re-arm.
+			etk_fifo_dead_streak = 0;
+		}
+
+		kill_itself = cfg_kill || storm_kill;
+
 		if (kill_itself)
 		{
 			fmt::throw_exception("Dead FIFO commands queue state has been detected!"
diff --git a/rpcs3/Emu/RSX/RSXThread.h b/rpcs3/Emu/RSX/RSXThread.h
index b8bc59ad8..ca681c2aa 100644
--- a/rpcs3/Emu/RSX/RSXThread.h
+++ b/rpcs3/Emu/RSX/RSXThread.h
@@ -317,6 +317,10 @@ namespace rsx
 		bool in_begin_end = false;
 
 		std::queue<desync_fifo_cmd_info> recovered_fifo_cmds_history;
+		// ETK anti-lock stage 3b (ffs-v4): consecutive "hopeless" FIFO-desync
+		// give-ups suppressed by GTK_FIFO_RESYNC before a clean stop. Reset on
+		// progress. See recover_fifo() in RSXThread.cpp.
+		u32 etk_fifo_dead_streak = 0;
 		std::deque<frame_time_t> frame_times;
 		u32 prevent_preempt_increase_tickets = 0;
 		u64 preempt_fail_old_preempt_count = 0;
diff --git a/rpcs3/Emu/RSX/VK/VKGSRender.cpp b/rpcs3/Emu/RSX/VK/VKGSRender.cpp
index e8acd2ec4..fd0ab4f99 100644
--- a/rpcs3/Emu/RSX/VK/VKGSRender.cpp
+++ b/rpcs3/Emu/RSX/VK/VKGSRender.cpp
@@ -892,6 +892,49 @@ VKGSRender::~VKGSRender()
 #endif
 }
 
+// ETK avwiden-v1 counters (defined in RSXThread.cpp namespace rsx::etk;
+// surfaced on the perfstat line by overlay_perf_metrics.cpp).
+namespace rsx
+{
+	namespace etk
+	{
+		extern atomic_t<u64> g_av_write_faults;
+		extern atomic_t<u64> g_av_read_faults;
+	}
+}
+
+// ETK avwiden-v1 (2026-07-12, GTK Edition v0.7.4). Evidence: GT HD at
+// LOCKED-60 burns 13.3 / 20.5 / 14.8% of ALL process cycles (three bog
+// samples, 2026-07-12) inside this handler — one PPU thread streaming
+// writes through texture-cache-protected pages, faulting page by page,
+// every frame. Mitigation: on a WRITE fault, additionally invalidate a
+// readahead span AHEAD of the faulting page so a linear writer takes one
+// fault per span instead of one per 4K page. Correctness: invalidation is
+// conservative by construction (worst case = extra texture revalidation);
+// the faulting page itself KEEPS the stock single-address call, preserving
+// exact violation_handled semantics (a widened-range hit on a neighbor
+// must never report the fault page as handled — that would refault
+// forever). Read faults stay precise (widening a read fault could flush
+// render targets wholesale). Default ON at 65536 (16 pages);
+// GTK_AV_READAHEAD=0 kill-switch, or a byte count (4K..16M, page-rounded).
+namespace
+{
+	u32 etk_av_readahead_bytes()
+	{
+		static const u32 bytes = []() -> u32
+		{
+			if (const char* v = std::getenv("GTK_AV_READAHEAD"))
+			{
+				const u64 parsed = std::strtoull(v, nullptr, 10);
+				if (parsed == 0) return 0;
+				if (parsed >= 4096 && parsed <= 16u * 1024 * 1024) return static_cast<u32>(parsed & ~0xfffull);
+			}
+			return 65536;
+		}();
+		return bytes;
+	}
+}
+
 bool VKGSRender::on_access_violation(u32 address, bool is_writing)
 {
 	rsx::mm_flush(address);
@@ -899,7 +942,25 @@ bool VKGSRender::on_access_violation(u32 address, bool is_writing)
 	vk::texture_cache::thrashed_set result;
 	{
 		const rsx::invalidation_cause cause = is_writing ? rsx::invalidation_cause::deferred_write : rsx::invalidation_cause::deferred_read;
+		(is_writing ? rsx::etk::g_av_write_faults : rsx::etk::g_av_read_faults)++;
 		result = m_texture_cache.invalidate_address(*m_secondary_cb_list.get(), address, cause);
+
+		// avwiden-v1 (doc block above): opportunistic readahead invalidation
+		// for the pages a linear writer will hit next. Result deliberately
+		// ignored — the stock call above owns violation_handled.
+		if (const u32 widen = etk_av_readahead_bytes(); widen && is_writing)
+		{
+			const u32 next_page = (address & ~0xfffu) + 0x1000;
+			if (next_page > address) // no wrap at the top of the address space
+			{
+				const u32 span = static_cast<u32>(std::min<u64>(widen, 0x1'0000'0000ull - next_page));
+				if (span)
+				{
+					m_texture_cache.invalidate_range(*m_secondary_cb_list.get(),
+						utils::address_range32::start_length(next_page, span), cause);
+				}
+			}
+		}
 	}
 
 	if (result.invalidate_samplers)
diff --git a/rpcs3/Emu/RSX/VK/VKGSRenderTypes.hpp b/rpcs3/Emu/RSX/VK/VKGSRenderTypes.hpp
index 0b80a5d4e..dc0e98b08 100644
--- a/rpcs3/Emu/RSX/VK/VKGSRenderTypes.hpp
+++ b/rpcs3/Emu/RSX/VK/VKGSRenderTypes.hpp
@@ -90,7 +90,28 @@ namespace vk
 				return false;
 			}
 
-			if (vkGetFenceStatus(pool->get_owner(), m_submit_fence->handle) == VK_SUCCESS)
+			VkResult fence_status = vkGetFenceStatus(pool->get_owner(), m_submit_fence->handle);
+			if (fence_status != VK_SUCCESS)
+			{
+				// ETK fence force-signal: this is the ONE poll site outside
+				// wait_for_fence — the hard-sync present-queue drain
+				// (`while (!m_queued_frames.empty()) check_present_status();`)
+				// spins HERE with no timeout and no escalation. Live-proven
+				// 2026-07-05 (GT HD, 00E5 flip wedge): post-hangcheck this
+				// fence never signals and the drain spins forever. Age the
+				// fence; at the threshold, drive through the normal
+				// completion path below exactly as if it signaled.
+				if (vk::etk_fence_note_not_ready(m_submit_fence))
+				{
+					fence_status = VK_SUCCESS;
+				}
+			}
+			else
+			{
+				vk::etk_fence_note_signaled(m_submit_fence);
+			}
+
+			if (fence_status == VK_SUCCESS)
 			{
 				lock.upgrade();
 
diff --git a/rpcs3/Emu/RSX/VK/VKPresent.cpp b/rpcs3/Emu/RSX/VK/VKPresent.cpp
index b5ba9c460..b8a8d9385 100644
--- a/rpcs3/Emu/RSX/VK/VKPresent.cpp
+++ b/rpcs3/Emu/RSX/VK/VKPresent.cpp
@@ -2,9 +2,11 @@
 #include "VKGSRender.h"
 #include "vkutils/buffer_object.h"
 #include "vkutils/memory.h"
+#include "vkutils/shared.h"
 #include "Emu/RSX/Overlays/overlay_manager.h"
 #include "Emu/RSX/Overlays/overlay_debug_overlay.h"
 #include "Emu/Cell/Modules/cellVideoOut.h"
+#include "Emu/System.h"
 
 #include "upscalers/bilinear_pass.hpp"
 #include "upscalers/fsr_pass.h"
@@ -129,7 +131,27 @@ bool VKGSRender::reinitialize_swapchain()
 
 	// Flush the command buffer
 	close_and_submit_command_buffer(&resize_fence);
-	vk::wait_for_fence(&resize_fence);
+	// ETK Tier 1 (787B reframe / Lead D, gt5p-boss-lsd-iteration 2026-07-01):
+	// second call site for the same fix as commands.cpp's command_buffer::begin().
+	// This was an unbounded wait_for_fence(&resize_fence) (default timeout=0) -
+	// live-confirmed 2026-07-01: a real a6xx fault (status=00E79587) left
+	// rsx::thread busy-spinning HERE, not at the commands.cpp site Tier 1
+	// originally patched, so the first patch alone didn't catch this hang.
+	// The boss can wedge at either fence wait depending on where in the frame
+	// (mid-draw vs. swapchain resize) it lands when the GPU is lost.
+	const VkResult wait_result = vk::wait_for_fence(&resize_fence, GENERAL_WAIT_TIMEOUT);
+	if (wait_result != VK_SUCCESS) [[unlikely]]
+	{
+		// Do NOT proceed to reset()/reuse the command buffer or swapchain state -
+		// the prior submission's GPU work is in an unknown state (timed out or
+		// the device is lost), so continuing now would race a still-possibly-
+		// live submission or corrupt state on the next frame.
+		rsx_log.fatal("GPU fence wait failed (VkResult=%d) after %uus during swapchain reinit - GPU is likely hung or lost. Stopping emulation.",
+			static_cast<s32>(wait_result), static_cast<u32>(GENERAL_WAIT_TIMEOUT));
+		vk::mark_device_lost();
+		Emu.Kill(false, false);
+		return false;
+	}
 
 	m_current_command_buffer->reset();
 	m_current_command_buffer->begin();
@@ -164,6 +186,14 @@ void VKGSRender::present(vk::frame_context_t *ctx)
 		default:
 			// Other errors not part of rpcs3. This can be caused by 3rd party injectors with bad code, of which we have no control over.
 			// Let the application attempt to recover instead of crashing outright.
+			if (error == VK_ERROR_DEVICE_LOST)
+			{
+				// Latch the loss at first sight so the reinit/teardown that
+				// follows never calls back into the dead device/layer chain
+				// (post-loss swapchain destroy segfaulted 2-for-2 on a6xx
+				// hangcheck recovery; see vkutils/shared.h teardown guard).
+				vk::mark_device_lost();
+			}
 			rsx_log.error("VkPresent returned unexpected error code %lld. Will attempt to recreate the swapchain. Please disable 3rd party injector tools.", static_cast<s64>(error));
 			swapchain_unavailable = true;
 			break;
diff --git a/rpcs3/Emu/RSX/VK/vkutils/commands.cpp b/rpcs3/Emu/RSX/VK/vkutils/commands.cpp
index 549d557af..17ee421f1 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/commands.cpp
+++ b/rpcs3/Emu/RSX/VK/vkutils/commands.cpp
@@ -3,6 +3,10 @@
 #include "shared.h"
 #include "sync.h"
 
+#include "Emu/RSX/VK/VKGSRenderTypes.hpp"
+#include "Emu/System.h"
+#include "util/logs.hpp"
+
 namespace vk
 {
 	// This queue flushing method to be implemented by the backend as behavior depends on config
@@ -87,7 +91,30 @@ namespace vk
 	{
 		if (m_submit_fence && is_pending)
 		{
-			wait_for_fence(m_submit_fence);
+			// ETK Tier 1 (787B reframe / Lead D, gt5p-boss-lsd-iteration 2026-07-01):
+			// this used to call wait_for_fence(m_submit_fence) with the default
+			// timeout=0, which spins forever on vkGetFenceStatus and NEVER routes
+			// through Turnip's msm_device_check_status fault-detection (only the
+			// bounded vkWaitForFences path does, per vk_common_WaitForFences ->
+			// vk_device_check_status). Live-confirmed on-rig 2x (different tracks,
+			// different a6xx fault signatures): rsx::thread busy-spins here
+			// (State: R, wchan=0) for the entire GPU-hang window with nothing
+			// watching, requiring a human R3 SIGKILL. A bounded wait converts an
+			// unrecoverable silent wedge into a detected, logged, clean stop.
+			const VkResult wait_result = wait_for_fence(m_submit_fence, GENERAL_WAIT_TIMEOUT);
+			if (wait_result != VK_SUCCESS) [[unlikely]]
+			{
+				// Do NOT proceed to reset()/reuse this command buffer - its prior
+				// submission's GPU work is in an unknown state (timed out or the
+				// device is lost), so resetting it now would race a still-possibly-
+				// live submission or corrupt state on the next recording pass.
+				rsx_log.fatal("GPU fence wait failed (VkResult=%d) after %uus - GPU is likely hung or lost. Stopping emulation.",
+					static_cast<s32>(wait_result), static_cast<u32>(GENERAL_WAIT_TIMEOUT));
+				vk::mark_device_lost();
+				Emu.Kill(false, false);
+				return;
+			}
+
 			is_pending = false;
 
 			//CHECK_RESULT(vkResetFences(pool->get_owner(), 1, &m_submit_fence));
diff --git a/rpcs3/Emu/RSX/VK/vkutils/shared.cpp b/rpcs3/Emu/RSX/VK/vkutils/shared.cpp
index c4a0219e6..c34d34ad7 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/shared.cpp
+++ b/rpcs3/Emu/RSX/VK/vkutils/shared.cpp
@@ -1,6 +1,9 @@
 #include "device.h"
 #include "shared.h"
 #include "util/logs.hpp"
+#include "Emu/System.h"
+
+#include <atomic>
 
 #ifndef _WIN32
 #include <signal.h>
@@ -10,6 +13,33 @@ namespace vk
 {
 	extern void print_debug_markers();
 
+	// Teardown guard latch: once the device is lost, destructors must not
+	// call back into the driver/layer chain (see shared.h).
+	static std::atomic<bool> g_device_lost_flag{ false };
+
+	void mark_device_lost()
+	{
+		bool expected = false;
+		if (g_device_lost_flag.compare_exchange_strong(expected, true))
+		{
+			// ETK tguard v4 (quit-on-lost): once the device is lost, any
+			// emulation stop must also end the PROCESS — in headless/no-gui
+			// use the Qt shell otherwise idles in exec() after Emu.Kill and
+			// the frontend launcher never regains control. Quit(true)
+			// bypasses the misc.autoexit config; riding after_kill_callback
+			// sequences it strictly after teardown completes.
+			Emu.after_kill_callback = []()
+			{
+				Emu.Quit(true);
+			};
+		}
+	}
+
+	bool is_device_lost()
+	{
+		return g_device_lost_flag.load();
+	}
+
 	std::string retrieve_device_fault_info()
 	{
 		if (!g_render_device || !g_render_device->get_extended_device_fault_support())
@@ -131,6 +161,7 @@ namespace vk
 			break;
 		case VK_ERROR_DEVICE_LOST:
 			error_message = "Device lost (Driver crashed with unspecified error or stopped responding and recovered) (VK_ERROR_DEVICE_LOST)";
+			mark_device_lost();
 			extra_info = retrieve_device_fault_info();
 			break;
 		case VK_ERROR_MEMORY_MAP_FAILED:
diff --git a/rpcs3/Emu/RSX/VK/vkutils/shared.h b/rpcs3/Emu/RSX/VK/vkutils/shared.h
index f8ffcc103..9b75a765b 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/shared.h
+++ b/rpcs3/Emu/RSX/VK/vkutils/shared.h
@@ -10,6 +10,13 @@ namespace vk
 
 	void die_with_error(VkResult error_code, std::string message = {}, std::source_location src_loc = std::source_location::current());
 
+	// Sticky device-loss latch (teardown guard). Set the moment any path sees
+	// VK_ERROR_DEVICE_LOST; consulted during teardown so destructors never
+	// call back into a dead device/layer chain (post-loss swapchain destroy
+	// segfaulted 2-for-2: null-PFN exec, and a torn layer-hook deref).
+	void mark_device_lost();
+	bool is_device_lost();
+
 	VKAPI_ATTR VkBool32 VKAPI_CALL dbgFunc(VkFlags msgFlags, VkDebugReportObjectTypeEXT objType,
 	                                       u64 srcObject, usz location, s32 msgCode,
 	                                       const char *pLayerPrefix, const char *pMsg, void *pUserData);
diff --git a/rpcs3/Emu/RSX/VK/vkutils/swapchain.cpp b/rpcs3/Emu/RSX/VK/vkutils/swapchain.cpp
index fe4464cb0..6502d8bec 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/swapchain.cpp
+++ b/rpcs3/Emu/RSX/VK/vkutils/swapchain.cpp
@@ -1,5 +1,6 @@
 #include "stdafx.h"
 #include "swapchain.h"
+#include "shared.h"
 
 namespace vk
 {
@@ -137,7 +138,14 @@ namespace vk
 	{
 		if (VkDevice pdev = dev)
 		{
-			if (m_vk_swapchain)
+			// Teardown guard: after a device loss aborts a swapchain reinit,
+			// this object can hold a swapchain handle with a null or torn
+			// dispatch pointer — calling it segfaulted 2-for-2 on live a6xx
+			// hangcheck recoveries (bare null-PFN exec, and a freed
+			// layer-hook deref with an overlay layer loaded). On a lost
+			// device the kernel reclaims driver objects at process exit;
+			// skipping the destroy call is correct, not a leak.
+			if (m_vk_swapchain && _vkDestroySwapchainKHR && !vk::is_device_lost())
 			{
 				_vkDestroySwapchainKHR(pdev, m_vk_swapchain, nullptr);
 			}
diff --git a/rpcs3/Emu/RSX/VK/vkutils/sync.cpp b/rpcs3/Emu/RSX/VK/vkutils/sync.cpp
index 6d36283ca..c03ad57e1 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/sync.cpp
+++ b/rpcs3/Emu/RSX/VK/vkutils/sync.cpp
@@ -7,10 +7,14 @@
 #include "shared.h"
 
 #include "Emu/Cell/timers.hpp"
+#include "Emu/RSX/Overlays/overlay_message.h"
 
 #include "util/sysinfo.hpp"
 #include "util/asm.hpp"
 
+#include <chrono>
+#include <cstdlib>
+
 namespace vk
 {
 	namespace globals
@@ -168,6 +172,10 @@ namespace vk
 	{
 		vkResetFences(owner, 1, &handle);
 		flushed.release(false);
+		// ETK: a reset fence starts a fresh life — clear the staleness stamp
+		// or reused fences would inherit age (same rule as t3devlost4).
+		etk_stale_since_us.release(0);
+		etk_stale_warned.release(false);
 	}
 
 	void fence::signal_flushed()
@@ -559,13 +567,147 @@ namespace vk
 		);
 	}
 
+	// ETK fence force-signal ("drive-through") — see the doc block in sync.h.
+	// The policy lives HERE, inside wait_for_fence, so every wait site in the
+	// backend (the tguard-bounded commands.cpp/VKPresent.cpp waits AND the
+	// legacy timeout=0 spins) inherits it without per-site edits: a forced
+	// fence returns VK_SUCCESS and the caller continues down its normal
+	// completion path. The one non-wait_for_fence poll site (poke() in
+	// VKGSRenderTypes.hpp, the GT HD hard-sync drain) calls the note_*
+	// helpers directly.
+	bool etk_fence_force_enabled()
+	{
+		static const bool enabled = []()
+		{
+			const char* v = std::getenv("GTK_FENCE_FORCE_SIGNAL");
+			return !(v && *v == '0'); // ETK v0.7.0: on by default; "0" = kill-switch
+		}();
+		return enabled;
+	}
+
+	static u64 etk_fence_force_threshold_us()
+	{
+		static const u64 threshold = []() -> u64
+		{
+			if (const char* v = std::getenv("GTK_FENCE_FORCE_SIGNAL_MS"))
+			{
+				if (const u64 ms = std::strtoull(v, nullptr, 10))
+				{
+					return ms * 1000ull;
+				}
+			}
+			// 5 s: far above any legitimate fence latency on this rig
+			// (frame fences signal in ms; shader-compile stalls are CPU-side
+			// and never hold a submitted fence this long), far below the
+			// operator's R3 reflex.
+			return 5'000'000ull;
+		}();
+		return threshold;
+	}
+
+	static u64 etk_steady_now_us()
+	{
+		return std::chrono::duration_cast<std::chrono::microseconds>(
+			std::chrono::steady_clock::now().time_since_epoch()).count();
+	}
+
+	static atomic_t<u64> g_etk_fence_forced_count = 0;
+
+	// ffs-v5 (v0.7.4): expose the forced-fence count so the RSX-side flip
+	// force-retire net can gate on "a rescue actually happened in this
+	// window" (RSXThread.cpp stage 4). Read-only, lock-free.
+	u64 etk_fence_forced_count()
+	{
+		return g_etk_fence_forced_count.load();
+	}
+
+	// Player-facing rescue notice (operator request 2026-07-05): the freeze is
+	// noticeable, so tell the driver to stay at the controls. Queued via
+	// RPCS3's native overlay the moment a stall crosses the warn threshold —
+	// the overlay renders through the present path, so the message physically
+	// appears at the FIRST frame after the rescue (kernel-alone recovery or
+	// force-signal), which is exactly when control returns.
+	static constexpr u64 ETK_STALL_WARN_US = 1'500'000;
+
+	static void etk_queue_rescue_notice(const char* text, u64 expiration_us)
+	{
+		rsx::overlays::queue_message(std::string(text), expiration_us);
+	}
+
+	bool etk_fence_note_not_ready(fence* pfence)
+	{
+		if (!etk_fence_force_enabled())
+		{
+			return false;
+		}
+
+		const u64 now = etk_steady_now_us();
+		const u64 since = pfence->etk_stale_since_us;
+		if (!since)
+		{
+			pfence->etk_stale_since_us.release(now);
+			return false;
+		}
+		if (now - since >= ETK_STALL_WARN_US && !pfence->etk_stale_warned.exchange(true))
+		{
+			etk_queue_rescue_notice("GTK Crash Recovery", 8'000'000);
+		}
+		if (now - since < etk_fence_force_threshold_us())
+		{
+			return false;
+		}
+
+		pfence->etk_stale_since_us.release(0);
+		pfence->etk_stale_warned.release(false);
+		const u64 n = ++g_etk_fence_forced_count;
+		rsx_log.error("ETK fence force-signal #%llu: fence stuck NOT_READY for >%llums (post-hangcheck orphan) - force-completing and continuing (GTK_FENCE_FORCE_SIGNAL).",
+			n, etk_fence_force_threshold_us() / 1000);
+		etk_queue_rescue_notice("GTK Crash Recovery", 5'000'000);
+		return true;
+	}
+
+	void etk_fence_note_signaled(fence* pfence)
+	{
+		const u64 since = pfence->etk_stale_since_us;
+		if (since)
+		{
+			// Kernel-alone survive: the fence came back on its own after a
+			// noticeable stall (the 2026-07-05 21:56 GT5P shape). Same
+			// driver-facing message as the forced path — the player doesn't
+			// care which layer caught it.
+			if (etk_steady_now_us() - since >= ETK_STALL_WARN_US)
+			{
+				etk_queue_rescue_notice("GTK Crash Recovery", 5'000'000);
+			}
+			pfence->etk_stale_since_us.release(0);
+			pfence->etk_stale_warned.release(false);
+		}
+	}
+
 	VkResult wait_for_fence(fence* pFence, u64 timeout)
 	{
 		pFence->wait_flush();
 
 		if (timeout)
 		{
-			return vkWaitForFences(*g_render_device, 1, &pFence->handle, VK_FALSE, timeout * 1000ull);
+			VkResult result = vkWaitForFences(*g_render_device, 1, &pFence->handle, VK_FALSE, timeout * 1000ull);
+			// ETK drive-through: don't hand VK_TIMEOUT to the caller (whose
+			// policy is Emu.Kill) while the force-signal window is still
+			// aging — keep waiting in bounded slices and force-complete at
+			// the threshold. Flag off => identical to stock (single wait).
+			while (result == VK_TIMEOUT && etk_fence_force_enabled())
+			{
+				if (etk_fence_note_not_ready(pFence))
+				{
+					return VK_SUCCESS; // forced — caller continues normally
+				}
+				result = vkWaitForFences(*g_render_device, 1, &pFence->handle, VK_FALSE, 250'000'000ull); // 250 ms slice
+			}
+			if (result == VK_SUCCESS)
+			{
+				etk_fence_note_signaled(pFence);
+			}
+			return result;
 		}
 		else
 		{
@@ -574,6 +716,10 @@ namespace vk
 				switch (status)
 				{
 				case VK_NOT_READY:
+					if (etk_fence_note_not_ready(pFence))
+					{
+						return VK_SUCCESS; // forced — see sync.h doc block
+					}
 					utils::pause();
 					continue;
 				default:
@@ -582,6 +728,7 @@ namespace vk
 				}
 			}
 
+			etk_fence_note_signaled(pFence);
 			return VK_SUCCESS;
 		}
 	}
diff --git a/rpcs3/Emu/RSX/VK/vkutils/sync.h b/rpcs3/Emu/RSX/VK/vkutils/sync.h
index 9177e8411..3fd0fc122 100644
--- a/rpcs3/Emu/RSX/VK/vkutils/sync.h
+++ b/rpcs3/Emu/RSX/VK/vkutils/sync.h
@@ -24,6 +24,16 @@ namespace vk
 		VkFence handle         = VK_NULL_HANDLE;
 		VkDevice owner         = VK_NULL_HANDLE;
 
+		// ETK fence force-signal ("drive-through", 2026-07-05): steady-clock us
+		// of the first NOT_READY observation on THIS fence; 0 = not stale.
+		// Cleared on every observed signal and on reset(), so per-frame fence
+		// reuse cannot age. See etk_fence_note_not_ready() in sync.cpp.
+		atomic_t<u64> etk_stale_since_us = 0;
+		// One-shot per stale episode: the "RESCUING FROM CRASH" player
+		// notification has been queued for this episode. Cleared with the
+		// stamp (signal/reset) so a later episode warns again.
+		atomic_t<bool> etk_stale_warned = false;
+
 		fence(VkDevice dev);
 		~fence();
 		fence(const fence&) = delete;
@@ -35,6 +45,21 @@ namespace vk
 		operator bool() const;
 	};
 
+	// ETK fence force-signal (GTK stack, 2026-07-05). Under the KGSL-parity
+	// kernel a hangcheck recovery keeps the GPU context alive, but a fence
+	// whose submission depended on the dropped submit will NEVER signal
+	// (proven live: GT HD frozen 20 s+, GPU idle, rsx spinning ~350% CPU).
+	// When armed (GTK_FENCE_FORCE_SIGNAL=1) a fence continuously NOT_READY
+	// past the threshold (GTK_FENCE_FORCE_SIGNAL_MS, default 5000) is
+	// force-completed at the emulator level: log, treat as signaled, present
+	// one garbage frame, keep racing. Default-off => behavior-identical to
+	// the tguard fast-exit build, so the two policies A/B via RPCS3_ENV_FLAGS
+	// without a rebuild.
+	bool etk_fence_force_enabled();
+	bool etk_fence_note_not_ready(fence* pfence);  // true => force-complete NOW
+	void etk_fence_note_signaled(fence* pfence);
+	u64 etk_fence_forced_count();                  // ffs-v5 rescue gate (RSXThread.cpp)
+
 	class event
 	{
 		enum class sync_backend
diff --git a/rpcs3/Emu/System.cpp b/rpcs3/Emu/System.cpp
index a9da0a7ff..cf54593a1 100644
--- a/rpcs3/Emu/System.cpp
+++ b/rpcs3/Emu/System.cpp
@@ -3333,8 +3333,28 @@ void Emulator::GracefulShutdown(bool allow_autoexit, bool async_op, bool savesta
 extern bool check_if_vdec_contexts_exist();
 extern bool try_lock_spu_threads_in_a_state_compatible_with_savestates(bool revert_lock = false, std::vector<std::pair<shared_ptr<named_thread<spu_thread>>, u32>>* out_list = nullptr);
 
+namespace vk { bool is_device_lost(); } // ETK tguard latch (vkutils/shared.cpp)
+
 void Emulator::Kill(bool allow_autoexit, bool savestate, savestate_stage* save_stage)
 {
+	// ETK tguard v6 (fast teardown, operator-directed 2026-07-05): on a lost
+	// device there is NOTHING worth tearing down gracefully — every polite
+	// path just walks dead Vulkan state for minutes (measured 90-116+ s
+	// fault-to-exit on the rig) while the driver answers DEVICE_LOST to
+	// every call. All persistent state that matters is already on disk:
+	// the log writer streams continuously (the fatal line is grep-able
+	// while the process is still alive), Mesa's shader disk cache writes
+	// during play, and game saves are transactional on their own schedule.
+	// The kernel reclaims everything else. Exit NOW; runemu returns, the
+	// frontend relaunches, and the session postmortem attributes the crash
+	// from on-disk evidence. Skipping destructors on a dead device is
+	// correct-by-construction (the London-pan precedent, 2026-07-05).
+	if (vk::is_device_lost())
+	{
+		sys_log.error("Device lost: fast teardown — exiting immediately (etk-tguard-v6).");
+		std::_Exit(0);
+	}
+
 	static const auto make_ptr = [](auto ptr)
 	{
 		return std::shared_ptr<std::remove_pointer_t<decltype(ptr)>>(ptr);
diff --git a/rpcs3/rpcs3.cpp b/rpcs3/rpcs3.cpp
index f0b508f56..931bbe338 100644
--- a/rpcs3/rpcs3.cpp
+++ b/rpcs3/rpcs3.cpp
@@ -91,6 +91,13 @@ static semaphore<> s_qt_init;
 atomic_t<bool> g_headless = false;
 static atomic_t<bool> s_no_gui = false;
 static atomic_t<char*> s_argv0 = nullptr;
+
+// Device-lost latch (vkutils/shared.cpp). Forward-declared to keep the
+// Vulkan headers out of the main TU; linked from the same binary.
+namespace vk
+{
+	bool is_device_lost();
+}
 static bool s_is_error_launch = false;
 
 std::string g_input_config_override;
@@ -371,8 +378,29 @@ public:
 					rsx::overlays::queue_message(overlay_msg, umax);
 				}
 
-				// Pause emulation if fatal error encountered
-				Emu.Pause(true);
+				// After a GPU device loss, Pause would freeze the session
+				// forever (nothing can resume on a dead device) and preempt
+				// the clean stop — observed live 2026-07-04 (Path B): a
+				// worker thread's DEVICE_LOST fatal landed here before the
+				// bounded-wait Kill path could run, leaving the game frozen
+				// on-screen with 83 live threads. The vk device-lost latch is
+				// set at first sight of the loss (before any fatal can fire),
+				// so route to a clean Kill instead; the guarded swapchain
+				// teardown handles the rest. Fire-once so a fatal storm
+				// during the Kill teardown cannot re-enter.
+				if (vk::is_device_lost())
+				{
+					static atomic_t<bool> s_devlost_kill_once = false;
+					if (!s_devlost_kill_once.exchange(true))
+					{
+						Emu.Kill(false, false);
+					}
+				}
+				else
+				{
+					// Pause emulation if fatal error encountered
+					Emu.Pause(true);
+				}
 			}
 		}
 	}
diff --git a/rpcs3/rpcs3_version.cpp b/rpcs3/rpcs3_version.cpp
index 49c40629f..4d2b0e0ed 100644
--- a/rpcs3/rpcs3_version.cpp
+++ b/rpcs3/rpcs3_version.cpp
@@ -50,6 +50,7 @@ namespace rpcs3
 	std::string get_verbose_version()
 	{
 		std::string version = fmt::format("%s | %s", rpcs3::get_version().to_string(), get_branch());
+		fmt::append(version, " | GTK Edition v0.8.1-dev");
 		if (is_local_build())
 		{
 			fmt::append(version, " | local_build");
